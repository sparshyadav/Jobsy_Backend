import { companyData } from "../types/services";

export const createJobService = async (data: companyData) => {
    const { name, website, industry, location, size, foundedBy, socialLinks } = data;
    
}